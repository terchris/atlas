# Concepts

In progress — one page per canonical entity (`kommune`, `ngo`, `chapter`, `activity`, `indicator`, `source`).

Track structure in [INVESTIGATE-semantic-foundation-before-expansion.md](../ai-developer/plans/backlog/INVESTIGATE-semantic-foundation-before-expansion.md). When the Concept Catalogue YAML lands, these pages become the auto-rendered output of the YAML.
