# Measurements

In progress — one page per `(source_id, contents_code)` pair (e.g. `ssb-08764` + `EUskala50`). Track structure in [INVESTIGATE-semantic-foundation-before-expansion.md](../ai-developer/plans/backlog/INVESTIGATE-semantic-foundation-before-expansion.md).
