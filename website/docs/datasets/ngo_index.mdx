---
title: "NGO registry"
description: "One row per organisation Atlas treats as an NGO."
slug: /datasets/ngo_index
sidebar_label: "ngo_index"
---

{/* Generated by website/scripts/generate-sources-registry.mjs — do not edit.
    Source of truth: atlas-data/ingest/src/sources/<id>/manifest.yml.
    To refresh, run: npm run sources:generate */}

import { viewById } from '@site/src/utils/sources';
import ViewHero from '@site/src/components/sources/ViewHero';
import ViewColumns from '@site/src/components/sources/ViewColumns';
import DatasetBuiltFrom from '@site/src/components/sources/DatasetBuiltFrom';
import SampleQueryUrl from '@site/src/components/sources/SampleQueryUrl';
import DatasetSamplePreview from '@site/src/components/sources/DatasetSamplePreview';

export const view = viewById("ngo_index");

<ViewHero view={view} />

<DatasetSamplePreview kind="view" apiV1Name={"ngo_index"} />

## Built from {#built-from}

This Atlas view joins data from the following upstream sources. Click through for provenance, methodology, and citation.

<DatasetBuiltFrom view={view} />

## Columns {#columns}

<ViewColumns view={view} />

## Sample query {#sample-query}

Live PostgREST query that returns the first 5 rows. URL adapts to the current host — local dev shows `api-atlas.localhost`; the deployed site shows `api-atlas.sovereignsky.no`.

<SampleQueryUrl url={view.sample_query} />

## About this view

<div style={{whiteSpace: 'pre-wrap'}}>{"One row per organisation Atlas treats as an NGO. The POPULATION is\nderived from Brreg — every organisation flagged\nregistrertIFrivillighetsregisteret, ~72,798 of them, reconciled\nevery half hour — unioned with the curated seed so no curated NGO\ncan drop out when the register disagrees. The EDITORIAL fields are\ncurated and are NULL for everything nobody has written up.\n\n🔴 This view grew from ~11 rows to ~72,798 on 2026-09-13\n(PLAN-003 phase 4.1). A consumer reading slug, tier, website_url,\nchapter_data_shape, has_chapters or primary_focus now gets NULL for\nalmost every row. Nothing was removed, but widening a view is not a\nbackward-compatible act just because no column disappeared —\nconsumers that assumed ~11 rows and populated editorial fields must\nfilter is_curated = true to get the previous behaviour.\n\nUse is_curated, NOT has_supply, to select the curated set.\nhas_supply answers \"does Atlas hold chapter data for this\norganisation\" (an ingest outcome); is_curated answers \"has anyone\nwritten this organisation up\" (an editorial fact). They agree today\nand are not the same question.\n\nRegister-sourced attributes — kommune, ICNPO category, bankruptcy\nstatus — are deliberately not copied here. Join api_v1.brreg_enhet\non orgnr for those.\n\nchapter_count counts only active chapters (dim_chapter.is_active),\nso a defunct NGO sits at 0 even if its historical chapters are\nretained."}</div>
