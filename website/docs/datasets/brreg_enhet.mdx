---
title: "api_v1.brreg_enhet"
description: "Every organisation registered in Norway — the whole of Brønnøysundregistrene's Enhetsregisteret, around 1.17 million rows, current as of the last change-feed…"
slug: /datasets/brreg_enhet
sidebar_label: "brreg_enhet"
---

{/* Generated by website/scripts/generate-sources-registry.mjs — do not edit.
    Source of truth: atlas-data/ingest/src/sources/<id>/manifest.yml.
    To refresh, run: npm run sources:generate */}

import { viewById } from '@site/src/utils/sources';
import ViewHero from '@site/src/components/sources/ViewHero';
import ViewColumns from '@site/src/components/sources/ViewColumns';
import DatasetBuiltFrom from '@site/src/components/sources/DatasetBuiltFrom';
import SampleQueryUrl from '@site/src/components/sources/SampleQueryUrl';
import DatasetSamplePreview from '@site/src/components/sources/DatasetSamplePreview';

export const view = viewById("brreg_enhet");

<ViewHero view={view} />

<DatasetSamplePreview kind="view" apiV1Name={"brreg_enhet"} />

## Built from {#built-from}

This Atlas view joins data from the following upstream sources. Click through for provenance, methodology, and citation.

<DatasetBuiltFrom view={view} />

## Columns {#columns}

<ViewColumns view={view} />

## Sample query {#sample-query}

Live PostgREST query that returns the first 5 rows. URL adapts to the current host — local dev shows `api-atlas.localhost`; the deployed site shows the configured API base.

<SampleQueryUrl url={view.sample_query} />

## About this view

<div style={{whiteSpace: 'pre-wrap'}}>{"Every organisation registered in Norway — the whole of Brønnøysundregistrene's\nEnhetsregisteret, around 1.17 million rows, current as of the last change-feed run.\nCompanies, foundations, associations, public bodies and sole proprietorships.\n\nKilde: Brønnøysundregistrene. Inneholder data under norsk lisens for offentlige data\n(NLOD) tilgjengeliggjort av Brønnøysundregistrene — https://data.norge.no/nlod/no/2.0.\nRe-users must carry this attribution onward; it is a condition of the licence, not a\ncourtesy.\n\nDeleted organisations are excluded. Brreg reports deletions through its change feed and\nAtlas applies them, so an organisation removed upstream disappears from here — but the\nversion history is kept, so what it looked like before removal remains answerable.\n\nCompleteness caveat: this reflects deletions Brreg REPORTS. A row Atlas holds that Brreg\nnever had would not be removed by any automatic path, because no change event would ever\nmention it."}</div>
