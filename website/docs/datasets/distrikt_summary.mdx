---
title: "Distrikt summary"
description: "One row per regional chapter (Red Cross \"distrikt\" level, and the equivalent regional tier for any other NGO with a hierarchy)."
slug: /datasets/distrikt_summary
sidebar_label: "distrikt_summary"
---

{/* Generated by website/scripts/generate-sources-registry.mjs — do not edit.
    Source of truth: atlas-data/ingest/src/sources/<id>/manifest.yml.
    To refresh, run: npm run sources:generate */}

import { viewById } from '@site/src/utils/sources';
import ViewHero from '@site/src/components/sources/ViewHero';
import ViewColumns from '@site/src/components/sources/ViewColumns';
import DatasetBuiltFrom from '@site/src/components/sources/DatasetBuiltFrom';
import SampleQueryUrl from '@site/src/components/sources/SampleQueryUrl';

export const view = viewById("distrikt_summary");

<ViewHero view={view} />

## Built from {#built-from}

This Atlas view joins data from the following upstream sources. Click through for provenance, methodology, and citation.

<DatasetBuiltFrom view={view} />

## Columns {#columns}

<ViewColumns view={view} />

## Sample query {#sample-query}

Live PostgREST query that returns the first 5 rows. URL adapts to the current host — local dev shows `api-atlas.localhost`; the deployed site shows `api-atlas.sovereignsky.no`.

<SampleQueryUrl url={view.sample_query} />

## About this view

<div style={{whiteSpace: 'pre-wrap'}}>{"One row per regional chapter (Red Cross \"distrikt\" level, and the\nequivalent regional tier for any other NGO with a hierarchy).\nEach row carries denormalised counts of how many child chapters\nthe distrikt has and how many distinct kommuner those children\ncover. Backs atlas-frontend /ngo/[slug]/distrikter and the\nindividual /distrikt/[id] pages; equivalent to the listDistrikter()\ninline query.\n\nCounts come from dim_chapter joined back to itself by\nparent_chapter_id, filtered to active children. PostgREST\nconsumers filter via ?ngo_orgnr=eq.X (or ?chapter_id=eq.Y for the\nsingle-distrikt case)."}</div>
