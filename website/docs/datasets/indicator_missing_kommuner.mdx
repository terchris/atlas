---
title: "Indicator coverage gaps per kommune"
description: "One row per (source_id, contents_code, kommune_nr) for every active kommune that has *no* non-NULL value at the indicator's latest_year."
slug: /datasets/indicator_missing_kommuner
sidebar_label: "indicator_missing_kommuner"
---

{/* Generated by website/scripts/generate-sources-registry.mjs — do not edit.
    Source of truth: atlas-data/ingest/src/sources/<id>/manifest.yml.
    To refresh, run: npm run sources:generate */}

import { viewById } from '@site/src/utils/sources';
import ViewHero from '@site/src/components/sources/ViewHero';
import ViewColumns from '@site/src/components/sources/ViewColumns';
import DatasetBuiltFrom from '@site/src/components/sources/DatasetBuiltFrom';
import SampleQueryUrl from '@site/src/components/sources/SampleQueryUrl';
import DatasetSamplePreview from '@site/src/components/sources/DatasetSamplePreview';

export const view = viewById("indicator_missing_kommuner");

<ViewHero view={view} />

<DatasetSamplePreview kind="view" apiV1Name={"indicator_missing_kommuner"} />

## Built from {#built-from}

This Atlas view joins data from the following upstream sources. Click through for provenance, methodology, and citation.

<DatasetBuiltFrom view={view} />

## Columns {#columns}

<ViewColumns view={view} />

## Sample query {#sample-query}

Live PostgREST query that returns the first 5 rows. URL adapts to the current host — local dev shows `api-atlas.localhost`; the deployed site shows `api-atlas.sovereignsky.no`.

<SampleQueryUrl url={view.sample_query} />

## About this view

<div style={{whiteSpace: 'pre-wrap'}}>{"One row per (source_id, contents_code, kommune_nr) for every\nactive kommune that has *no* non-NULL value at the indicator's\nlatest_year. The \"coverage gap\" sidebar on the data explorer\ndetail page; equivalent to the listMissingKommuner() inline query.\n\nPostgREST consumers filter via\n?source_id=eq.X&contents_code=eq.Y. Membership semantics: a\nkommune is considered \"missing\" if it either has no row at\nlatest_year, or has a row with value IS NULL. Status markers (':',\n'X') do not count as a value.\n\nThe full table is the cross-product of every indicator with every\nactive kommune that lacks data. Most cells are present, so the\nunfiltered row count is the per-indicator gap-list aggregated\nacross all indicators."}</div>
