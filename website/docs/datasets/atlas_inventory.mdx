---
title: "api_v1.atlas_inventory"
description: "What Atlas publishes, one row per queryable endpoint: how many records it serves, when its data last arrived, and where those rows came from."
slug: /datasets/atlas_inventory
sidebar_label: "atlas_inventory"
---

{/* Generated by website/scripts/generate-sources-registry.mjs — do not edit.
    Source of truth: atlas-data/ingest/src/sources/<id>/manifest.yml.
    To refresh, run: npm run sources:generate */}

import { viewById } from '@site/src/utils/sources';
import ViewHero from '@site/src/components/sources/ViewHero';
import ViewColumns from '@site/src/components/sources/ViewColumns';
import DatasetBuiltFrom from '@site/src/components/sources/DatasetBuiltFrom';
import SampleQueryUrl from '@site/src/components/sources/SampleQueryUrl';
import DatasetSamplePreview from '@site/src/components/sources/DatasetSamplePreview';

export const view = viewById("atlas_inventory");

<ViewHero view={view} />

<DatasetSamplePreview kind="view" apiV1Name={"atlas_inventory"} />

## Built from {#built-from}

This Atlas view joins data from the following upstream sources. Click through for provenance, methodology, and citation.

<DatasetBuiltFrom view={view} />

## Columns {#columns}

<ViewColumns view={view} />

## Sample query {#sample-query}

Live PostgREST query that returns the first 5 rows. URL adapts to the current host — local dev shows `api-atlas.localhost`; the deployed site shows the configured API base.

<SampleQueryUrl url={view.sample_query} />

## About this view

<div style={{whiteSpace: 'pre-wrap'}}>{"What Atlas publishes, one row per queryable endpoint: how many records it\nserves, when its data last arrived, and where those rows came from.\n\nAsked for by Røde Kors — \"validate that all datasets that are ingested has\nan endpoint and it can be queried … count the number of records each\nendpoint has and update the last time it ingested data.\"\n\n⚠️ THE GRAIN IS PER ENDPOINT, NOT PER SOURCE. meta_sources answers \"what\ndid Atlas ingest\"; this answers \"what can I query, and is there anything\nin it\". One source can feed several endpoints and one endpoint can draw on\nseveral sources, so the two row counts are not comparable and summing\nsources will not reproduce an endpoint's count.\n\n⚠️ atlas_inventory does not list ITSELF. It is built from the same seed it\nreports on, so counting itself would be circular and the number would be\nwhatever the table held mid-build. 19 endpoints are listed; 20 are\npublished."}</div>
