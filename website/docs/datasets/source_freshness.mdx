---
title: "Source freshness"
description: "One row per raw source table that declares a `loaded_at_field`, saying whether it is inside the window its own declared cadence allows."
slug: /datasets/source_freshness
sidebar_label: "source_freshness"
---

{/* Generated by website/scripts/generate-sources-registry.mjs — do not edit.
    Source of truth: atlas-data/ingest/src/sources/<id>/manifest.yml.
    To refresh, run: npm run sources:generate */}

import { viewById } from '@site/src/utils/sources';
import ViewHero from '@site/src/components/sources/ViewHero';
import ViewColumns from '@site/src/components/sources/ViewColumns';
import DatasetBuiltFrom from '@site/src/components/sources/DatasetBuiltFrom';
import SampleQueryUrl from '@site/src/components/sources/SampleQueryUrl';
import DatasetSamplePreview from '@site/src/components/sources/DatasetSamplePreview';

export const view = viewById("source_freshness");

<ViewHero view={view} />

<DatasetSamplePreview kind="view" apiV1Name={"source_freshness"} />

## Built from {#built-from}

This Atlas view joins data from the following upstream sources. Click through for provenance, methodology, and citation.

<DatasetBuiltFrom view={view} />

## Columns {#columns}

<ViewColumns view={view} />

## Sample query {#sample-query}

Live PostgREST query that returns the first 5 rows. URL adapts to the current host — local dev shows `api-atlas.localhost`; the deployed site shows the configured API base.

<SampleQueryUrl url={view.sample_query} />

## About this view

<div style={{whiteSpace: 'pre-wrap'}}>{"One row per raw source table that declares a `loaded_at_field`, saying\nwhether it is inside the window its own declared cadence allows.\n\nThis is the operator-readable half of the ingest-freshness signal. The\ncomparison used to live only inside the dbt test\n`raw_sources_were_refreshed_recently`, whose verdict exists only while the\nsuite is running — it lands in dbt's run results and Dagster's event log,\nand `atlas-status.py` can read neither. So `uis template check atlas`\nreported a 24-hour window instead, which cannot see a weekly or monthly\nsource go stale at all (urb-agents #1039).\n\nIt is a VIEW on purpose: evaluated when queried, so it is current whether\nor not the transform, the check suite or the Dagster daemon has run. A\ntable would carry the very staleness it is meant to report.\n\nThe test now selects its failing rows from here rather than recomputing\nthem, so the gate and the surface cannot disagree. Cadence bounds are\n`vars.ingest_cadence_max_age_days` in dbt_project.yml, read by both and\ndefaulted by neither."}</div>
