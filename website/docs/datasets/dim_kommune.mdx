---
title: "Kommune dimension"
description: "The canonical municipality registry — SSB Klass 131, with fylke name joined."
slug: /datasets/dim_kommune
sidebar_label: "dim_kommune"
---

{/* Generated by website/scripts/generate-sources-registry.mjs — do not edit.
    Source of truth: atlas-data/ingest/src/sources/<id>/manifest.yml.
    To refresh, run: npm run sources:generate */}

import { viewById } from '@site/src/utils/sources';
import ViewHero from '@site/src/components/sources/ViewHero';
import ViewColumns from '@site/src/components/sources/ViewColumns';
import DatasetBuiltFrom from '@site/src/components/sources/DatasetBuiltFrom';
import SampleQueryUrl from '@site/src/components/sources/SampleQueryUrl';
import DatasetSamplePreview from '@site/src/components/sources/DatasetSamplePreview';

export const view = viewById("dim_kommune");

<ViewHero view={view} />

<DatasetSamplePreview kind="view" apiV1Name={"dim_kommune"} />

## Built from {#built-from}

This Atlas view joins data from the following upstream sources. Click through for provenance, methodology, and citation.

<DatasetBuiltFrom view={view} />

## Columns {#columns}

<ViewColumns view={view} />

## Sample query {#sample-query}

Live PostgREST query that returns the first 5 rows. URL adapts to the current host — local dev shows `api-atlas.localhost`; the deployed site shows the configured API base.

<SampleQueryUrl url={view.sample_query} />

## About this view

<div style={{whiteSpace: 'pre-wrap'}}>{"The canonical municipality registry — SSB Klass 131, with fylke\nname joined. Published as api_v1.dim_kommune.\n\nFourteen references in this file already pointed at dim_kommune —\nfive column descriptions saying \"FK to dim_kommune\" and the rest\n`relationships` tests — while it was not published. A consumer\nbuilding on the API found the gap and called publishing it \"the\nsingle highest-leverage small addition on this list — every\nconsumer is currently rebuilding it, and each of us is rebuilding\nit slightly differently\" (urb-agents #1250, finding 4).\n\nHistorical codes are included with is_active = false, because a\nconsumer joining older data needs them to resolve. Filter\n?is_active=is.true for today's 357.\n\nPopulation and centroids are deliberately absent — see the model\nfor why; both would be wrong in a dimension rather than merely\nmissing."}</div>
