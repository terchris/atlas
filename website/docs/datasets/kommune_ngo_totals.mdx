---
title: "Voluntary organisations per kommune, total"
description: "One row per kommune: how many active voluntary organisations are registered there."
slug: /datasets/kommune_ngo_totals
sidebar_label: "kommune_ngo_totals"
---

{/* Generated by website/scripts/generate-sources-registry.mjs — do not edit.
    Source of truth: atlas-data/ingest/src/sources/<id>/manifest.yml.
    To refresh, run: npm run sources:generate */}

import { viewById } from '@site/src/utils/sources';
import ViewHero from '@site/src/components/sources/ViewHero';
import ViewColumns from '@site/src/components/sources/ViewColumns';
import DatasetBuiltFrom from '@site/src/components/sources/DatasetBuiltFrom';
import SampleQueryUrl from '@site/src/components/sources/SampleQueryUrl';
import DatasetSamplePreview from '@site/src/components/sources/DatasetSamplePreview';

export const view = viewById("kommune_ngo_totals");

<ViewHero view={view} />

<DatasetSamplePreview kind="view" apiV1Name={"kommune_ngo_totals"} />

## Built from {#built-from}

This Atlas view joins data from the following upstream sources. Click through for provenance, methodology, and citation.

<DatasetBuiltFrom view={view} />

## Columns {#columns}

<ViewColumns view={view} />

## Sample query {#sample-query}

Live PostgREST query that returns the first 5 rows. URL adapts to the current host — local dev shows `api-atlas.localhost`; the deployed site shows the configured API base.

<SampleQueryUrl url={view.sample_query} />

## About this view

<div style={{whiteSpace: 'pre-wrap'}}>{"One row per kommune: how many active voluntary organisations are\nregistered there. 357 rows instead of the ~5 400 in\nkommune_ngo_summary. Published as api_v1.kommune_ngo_totals.\n\nExists because PostgREST cannot sum — `?select=active_count.sum()`\nreturns PGRST123, aggregates are disabled — so a consumer wanting\none number per kommune otherwise fetches every category row and\nadds them client-side. That was most of a cold page load\n(urb-agents #1265).\n\n⚠️ Counts exclude the 10.3 % of active voluntary units carrying no\nkommune_nr, exactly as kommune_ngo_summary does. Summing this gives\nthe placed total, not the register total.\n\n⚠️ COST: filtered by kommune_nr this is sub-second. UNFILTERED it\naggregates the whole register — 25 s cold before the covering\nindex on dim_brreg_enhet, and that index is what makes enumerating\nall rows viable at all. If it is ever dropped, this relation\nbecomes unusable rather than slow."}</div>
