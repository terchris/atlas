---
title: "api_v1.meta_endpoints"
description: "One row per queryable Atlas endpoint, with tags inherited from upstream sources via the lineage graph and a `layer:<schema>` tag from the schema."
slug: /datasets/meta_endpoints
sidebar_label: "meta_endpoints"
---

{/* Generated by website/scripts/generate-sources-registry.mjs — do not edit.
    Source of truth: atlas-data/ingest/src/sources/<id>/manifest.yml.
    To refresh, run: npm run sources:generate */}

import { viewById } from '@site/src/utils/sources';
import ViewHero from '@site/src/components/sources/ViewHero';
import ViewColumns from '@site/src/components/sources/ViewColumns';
import DatasetBuiltFrom from '@site/src/components/sources/DatasetBuiltFrom';
import SampleQueryUrl from '@site/src/components/sources/SampleQueryUrl';
import DatasetSamplePreview from '@site/src/components/sources/DatasetSamplePreview';

export const view = viewById("meta_endpoints");

<ViewHero view={view} />

<DatasetSamplePreview kind="view" apiV1Name={"meta_endpoints"} />

## Built from {#built-from}

This Atlas view joins data from the following upstream sources. Click through for provenance, methodology, and citation.

<DatasetBuiltFrom view={view} />

## Columns {#columns}

<ViewColumns view={view} />

## Sample query {#sample-query}

Live PostgREST query that returns the first 5 rows. URL adapts to the current host — local dev shows `api-atlas.localhost`; the deployed site shows the configured API base.

<SampleQueryUrl url={view.sample_query} />

## About this view

<div style={{whiteSpace: 'pre-wrap'}}>{"One row per queryable Atlas endpoint, with tags inherited from\nupstream sources via the lineage graph and a `layer:<schema>` tag\nfrom the schema. Wraps to `api_v1.meta_endpoints` and backs the\ntag-filter catalogue at `/data` in the customer frontend\n(PLAN-007 phase 4).\n\n🔵 THIS IS THE INDEX, SO IT SAYS WHERE THE OTHER TWO CATALOGUES\nARE: `meta_sources` for one row per ingest source (licence,\npublisher, freshness), and `meta_dimensions` for one row per\nsource × upstream dimension — what each coded column means and\nits value format. A consumer enumerating relations here should\nnot have to read 19 descriptions to discover the second one\n(urb-agents #1335).\n\nIncludes endpoints from `api_v1.*`, `marts.*`, and `raw.*`\n(Atlas's three open-by-default schemas; `private_marts.*` stays\nauth-gated and is excluded). Skips internal seeds prefixed `_`\n(`_sources_manifest`, `_sources_dimensions`) and any `dbt_*`\ndiagnostic tables.\n\nTag inheritance uses the **union** rule: a `mart_*` derived from\nmany sources picks up every source's `provider:`, `topic:`,\n`geo:`, `cadence:`, and `eu_theme:` tag, deduped. Filter\n\"involves something annual\" beats \"purely annual\" — see PLAN-007\nphase 3.2 for the design rationale."}</div>
