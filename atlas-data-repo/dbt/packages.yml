packages:
  # dbt_utils — macros for unique-combination tests, unions across models, and
  # accepted-range tests. One of the seven dbt patterns we're standardising on.
  - package: dbt-labs/dbt_utils
    version: [">=1.2.0", "<2.0.0"]
